# Local Covira backend environment
DB_URL=jdbc:postgresql://localhost:5432/covira_db
DB_USERNAME=postgres
DB_PASSWORD=YOUR_POSTGRES_PASSWORD

# Optional: create the five demo candidates on first startup
COVIRA_DEMO_DATA=true

# Mail settings (required only for email features)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=YOUR_EMAIL
MAIL_PASSWORD=YOUR_APP_PASSWORD
